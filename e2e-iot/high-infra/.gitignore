# Terraform
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
# terraform.tfvars - commented out to allow tracking mandatory terraform.tfvars
# *.tfvars.backup

# Vendored Fluss Helm chart (helm-charts/fluss/) is tracked — includes persistence support.

# Local cluster credentials and plan artifacts (never commit)
terraform/kubeconfig
terraform/tfplan
terraform/tfplan-*
terraform/*.tfstate
terraform/*.tfstate.*

# IDE
.idea/
.vscode/
*.swp
*.swo

